def resolve_sales_dataset(geo):
        """
        Resolve the dataset name based on the provided geo parameter.
        
        Args:
            geo (str): The geo parameter.
        
        Returns:
            str: The resolved sales dataset name.
        """
        # Example resolution logic (replace with your own logic)
        dataset_name = f"amazon_sales_{geo}"
        return dataset_name

def resolve_all_dataset():
        datasets = ["amazon_sales_in"]  # Example list of dataset names
        return datasets
        